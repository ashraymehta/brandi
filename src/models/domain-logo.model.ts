export class DomainLogo {
  public readonly domain: string;
  public readonly logoKey: string;

  constructor(domain: string, logoKey: string) {
    this.domain = domain;
    this.logoKey = logoKey;
  }
}
